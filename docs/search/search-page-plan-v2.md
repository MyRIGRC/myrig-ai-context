# /search「探す」現行仕様 v2 — search-page-plan-v1を全面置換（supersedes: v1）

**作成:** 2026-07-23 (JST) / Claude
**最終更新:** 2026-08-22 — **正典⇄モック照合で実装（P22-C29「D案」）へ全面追随**
v1の旧記述（ページ遷移型ヘッダー検索・自動focus・ページ内大型SearchField・ページネーション）はすべて失効。

> **本書の位置づけ（2026-08-21 追記）**
> モックで進めながら正典が追随する運用のため、**実装が実態**。本書は実装を写したもの。
> 実装との差が出たら**実装が正**として本書を改訂すること。
> 対応する実装ファイル: `App/MOKUP/myrig_pc_Ver3/search.html` / `search-results.html` /
> `js/mobile-shell.js`（Header Search Overlay）/ `pc/myrig-search-v3.html`（PC）
> ※実装の `/search` はモックでは `search.html`、`/search?q=` は `search-results.html?q=` に読み替え

---

## 1. 導線3本（確定・変更なし）

| # | 導線 | 挙動 |
|---|---|---|
| ① | **Header検索** | その場でOverlay（契約§3.1b）。**遷移しない**。`js/mobile-shell.js:406 openHeaderSearch()` |
| ② | **BottomNav「探す」** | `/search` へ移動。**自動focusなし**（カテゴリやメーカーから探す動線を妨げないため） |
| ③ | **確定** | `/search?q=` へ移動 |

Overlay の空欄時は**最近の検索のみ**。「注目の検索キーワード」等のトレンド表示は置かない
（`js/mobile-shell.js:381` に明記・ランキング全廃方針と整合）。

---

## 2. /search トップ（`search.html`）— **P22-C29「D案」で確定**

> ⚠️ **2026-08-21 改稿**: 旧記述の「種別で探す → 人・ガレージを探す独立行 → 車種カテゴリ →
> パーツカテゴリ → メーカーから探す」という**一列構成はもう存在しない**。
> 2026-08-21 P22-C29 で「探し方4枚＋軸の開閉」へ移行済み（`search.html:203` 改訂履歴）。

### 構成（上から）

1. **ロゴ**（現在地表示。戻る矢印ではないので「主要タブを下層ページに見せない」契約§3.5に抵触しない）
2. **SearchHeader variant**（検索欄をヘッダーへ統合・戻るなし）
3. **最近の検索**
4. **探し方 4枚**（`search.html:244-266`）
5. **選んだ探し方に応じたパネル**（同一画面内で開く。別ページへ遷移しない）
6. **「条件で探す」パネル内の開閉軸 5本**
7. **固定バー「この条件で検索」**（`search.html:476-478`）

### 探し方 4枚（`data-how`）

| `data-how` | ラベル | パネル |
|---|---|---|
| `cond` | 条件で探す | 開閉軸5本（下記） |
| `pn` | 型番で探す | `#pnlPn` |
| `myrig` | 自分のRIGから探す | `#pnlMyrig` |
| `user` | 人・ガレージを探す | `#pnlUser` |

> **「人・ガレージを探す」は独立行ではない。** 旧 `.user-search-row`（独立行・別ページ遷移）から
> 探し方4枚の1枚へ移設済み（`search.html:259` に移行コメント）。
> 「型番で探す」「自分のRIGから探す」は v2 初版の時点で正典から欠落していた導線。

### 「条件で探す」の開閉軸 5本（`data-ax`）

| `data-ax` | 見出し | 備考 |
|---|---|---|
| `type` | 種別 | RIG / パーツ / LOG / 製品情報 の**4種**（ユーザーは含まない＝探し方カード側） |
| `scat` | 車種カテゴリ | 24件 |
| `pcat` | パーツカテゴリ | 親14件 |
| `maker` | メーカー | |
| `scale` | スケール | 13値（`data-size-class`） |

「すべて開く」トグルあり（`search.html:296-298`）。

### 置かないもの

- **人気検索・トレンド・急上昇のセクション**（ランキング全廃方針）
  `search.html` 側の該当語は0件

> ⚠️ **ただし結果ページに「人気」表記が残っている**（2026-08-22 検出・**要裁定**）
>
> | 場所 | 内容 | 性質 |
> |---|---|---|
> | `search-results.html:621` | `<span class="sr-full__badge">人気</span>` | **ユーザーに描画されるUI文言** |
> | `search-results.html:498` | FEATURED プールのコメント「人気投稿」 | コメント |
> | `search-results.html:617` | 「6件に1枚・人気バッジ」 | コメント |
>
> §3 の「**『人気順』は意図的に使わない**（裁定⑦）」および ranking 全廃方針と整合していない。
> FEATURED 枠のバッジ文言を「注目」等へ変えるか、「人気」は順位ではなく単なるラベルとして許容するか、
> **イタヤ裁定が必要**。→ モック是正キューへ。

---

## 3. 検索結果（`search-results.html`）

### ヘッダー・ナビ

- **SearchHeader**（戻る = fallback `/search`。`history.back()` 不能時は `search.html` へ）
- **種別 6本・件数つき**: すべて84 / RIG18 / パーツ18 / LOG18 / 製品情報18 / ユーザー12
  > ⚠️ 実装は `role="tablist"` のタブではなく **`.filter-chip[data-type]` のチップ列**（非sticky）。
  > 「タブ」という呼称は実装と一致しないが、慣用として本書では種別タブと呼ぶ。

### ソート — **2026-08-21 イタヤ裁定⑦⑧で全面改訂**

> ⚠️ **旧記述「関連度・新着・人気の3ピル」は廃止済み。**
> `search-results.html:116-122` に「旧: 3ピルを"すべて"でも常時表示していた…行ごと出さないことにした」と記録。

| 面 | 挙動 |
|---|---|
| 「すべて」タブ | **ソート行そのものを非表示**（`:722` `sortRow.hidden = (type==='all')`）。横断ソートは定義不能として撤去 |
| 種別タブ | **単一ドロップダウン**（`:123-127`。中身はPhase2） |

**「人気順」は意図的に使わない**（裁定⑦）。種別ごとの選択肢（`:569-575`）:

| 種別 | 選択肢 |
|---|---|
| RIG | 関連順 / 新着順 / いいね順 |
| パーツ | 関連順 / 装着例が多い順 / 新着順 |
| LOG | 関連順 / 新着順 / いいね順 |
| 製品情報 | 関連順 / 登録RIGが多い順 / 新着順 |
| ユーザー | 関連順 / RIG登録数順 |

既定は全種別「関連順」（`:576 SORT_DEFAULT`）。

### 絞り込み

- **絞り込みSheet** `#filterSheet`（`:268-271`）
- **件数バッジ** `#filterCount`（`:130`）
- **適用中条件チップ** `#appliedBar`（`:136-139`）

### 「すべて」= 増量ダイジェスト29件

`search-results.html:563` `DIGEST = { rig:8, parts:8, lib:4, log:6, user:3 }`

**表示順は rig → parts → lib → log → user**（`:637`）。各セクションに「◯◯の結果をすべて見る →」。
**無限にしない**（`:633`「概観の役割」）。

> ⚠️ **要裁定**: 種別チップの並び（`:107-112` = all, rig, parts, **log, lib**, user）と
> ダイジェストの並び（`:637` = rig, parts, **lib, log**, user）で **log と lib が入れ替わっている**。
> どちらかに揃えるべきだが、実装がそうなっている理由は不明。

### 種別タブ = 無限スクロール

`:653-694` `BATCH_COUNT=3, PER_BATCH=6`、IntersectionObserver、終端「すべて表示しました」。
ページ送りUIは廃止（`:147`）。

> ⚠️ **2026-08-21 監査**: #26 の「モバイル一覧のページネーション**原則廃止**」という広い読みは、
> 同日の #25「他ページ（検索結果・一覧系）はページネーション維持のまま」および
> pc-mobile-spec-inheritance G7「ページング／もっと見るが基本」と対立する。**要調停（HOLD）。**
> **検索の種別タブが無限スクロールであること自体は確定**で、この裁定は動かない。
> 未確定なのは「検索タブ以外の一覧系まで無限スクロールに広げるか」。

### FEATURED 別枠

`:498-501` FEATURED プール（検索結果18件とは**別枠**の注目枠）＋ `:655` `FULL_AT=4`
（4件目直後に差し込み）。

**ユーザータブには差し込まない**（`:663`「ユーザーは投稿ではないため差し込まない」）。

### 表示形式

`:560` `GRIDTYPE = { rig:1, parts:1, lib:1 }` → RIG / パーツ / 製品情報 = **2列グリッド**（`.pgrid2`）、
LOG / ユーザー = **リスト行**（`.sr-list`）。

空状態 = `.m-empty`（`:158-161`。`?empty=1` でデモ）＋ CTA「カテゴリから探す ›」→ `search.html`。

### 広告枠

**検索面は広告ゼロ**（2026-08-21 イタヤ裁定①）。AD/Affiliate枠は撤去済み（`:149-154`）。

---

## 4. 色の扱い

- **検索ハイライトは `--cat-*` 不使用。** 汎用accent系 `--color-link` を使う
  （`css/mobile-shell.css:2172-2179`「カテゴリtoken(--cat-log)の流用は…排除し汎用accent系へ変更」）
- **ただし種別チップの選択色は `--cat-*` が正**（`search.html:57-59`）。
  これはカテゴリ表現そのものであって流用ではない

> 禁止の対象は**ハイライトのみ**。種別チップは `--cat-*` 使用が正しい、という線引き。

---

## 5. 検索スコープ定義 — **実装に一元定義が存在する**

> 旧「未確定（正典側の宿題）」から格上げ。`search-results.html:524-576` の定数群が事実上の一元定義。

| 定数 | 行 | 内容 |
|---|---|---|
| `LABEL` | :524 | RIG / パーツ / LOG / 製品情報 / ユーザー |
| `UNIT` | :559 | 台 / 点 / 件 / 件 / 人 |
| `GRIDTYPE` | :560 | rig, parts, lib = グリッド |
| `DIGEST` | :563 | rig8, parts8, lib4, log6, user3 |
| `SORT_OPTIONS` | :569 | 上記§3のとおり |
| `SORT_DEFAULT` | :576 | 全種別「関連順」 |

**注意**: `search.html:302-315` の「種別」軸は **4種（rig/parts/log/lib）でユーザーを含まない**。
ユーザーは探し方カード側にある。スコープ5種のうち1つが軸から外れている構造。

---

## 6. プレースホルダ文言

**基準文言: `RIG・パーツ・LOG・製品情報を検索`**

| 面 | 実装 |
|---|---|
| モバイル `search.html:224` / `search-results.html:93` | ✅ 基準文言 |
| PC ヘッダー `pc/myrig-search-v3.html:677` | ✅ 基準文言 |
| **PC 結果ページ内の再検索 `pc/myrig-search-v3.html:1173`** | ❌ `RIG・パーツ・ログ・ユーザーを検索`（**未統一**） |

モバイルは探し方でスコープ連動の可変文言を持つ（`search.html:614-619`）:

| 探し方 | 文言 |
|---|---|
| cond / myrig | RIG・パーツ・LOG・製品情報を検索 |
| pn | 型番・製品番号を入力（例: TRX-4） |
| user | ユーザー名・ガレージ名を検索 |

> **残作業（モック側）**: `pc/myrig-search-v3.html:1173` を基準文言へ。
> 旧「未確定」項目だが、実際はモバイルが統一済みでPCだけ取り残されている状態。

---

## 7. 未確定（正典側の宿題）

- **関連度の算出定義** — 正典にも実装にも無い。実装は `search-results.html:567` で「Phase1 は既定値の
  ラベル表示のみ。実装はPhase2」と明記
- **種別チップ順とダイジェスト順の log/lib 入れ替わり**（上記§3）— どちらに揃えるか要裁定
- **検索軸に関わる値集合のHOLD** — `size_class`（実データ18パターン）/ `event_tags`（列が
  DBに存在しない）。CURRENT.md のHOLD欄参照

### 解決済み（旧「未確定」から移動）

- ~~検索スコープ正典表~~ → §5 に実装から昇格
- ~~種別混在ルール~~ → 「すべて」タブのみセクション分割ダイジェスト。横断ソートは定義不能として撤去
- ~~PC側プレースホルダ2種の統一~~ → §6 に基準文言を確定。残るのはモック側の1箇所修正のみ

---

## 8. 実装との既知の差分（モック側の残作業）

| # | 内容 | 場所 |
|---|---|---|
| 1 | PCプレースホルダが未統一 | `pc/myrig-search-v3.html:1173` |
| 2 | 48px未満のタップ要素4件 | `search.html:114 .rc__chip`(30px) / `:120-121 .rc__x`(44px) / `:122-124 .rc__clear` / `:161-162 .ax-tools__b` |
| 3 | `data-pcat` とラベルの不一致 | `search.html:376` `メンテ・工具・ピット` vs ラベル `メンテ・工具` |
| 4 | 古い注記 | `pc/myrig-search-v3.html:839`「モバイル search.html の21件・タイル12件は誤り」は**注記が古い**（実装は24件に是正済み） |
