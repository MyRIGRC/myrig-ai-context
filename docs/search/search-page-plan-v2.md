# /search「探す」現行仕様 v2

> **拘束力: L2（現在の確定仕様・より良い案の提案歓迎）**
>
> いまモックアップ制作フェーズ。デザインとサービス概念を**議論しながら作る**段階なので、
> 本書は「今こうなっている」という出発点であって、議論の打ち切りではない。
> **「既存仕様と異なる」ことだけを理由に案を捨てないこと。**
> 差分を明示すればイタヤ裁定で変更できる。

**更新:** 2026-08-23（検索基盤の実装原則を追加）
**実装:** `search.html` / `search-results.html` / `js/mobile-shell.js` / `pc/myrig-search-v3.html`
モックでは `/search`→`search.html`、`/search?q=`→`search-results.html?q=` に読み替え。

---

## 導線3本

| # | 導線 | 挙動 |
|---|---|---|
| ① | Header検索 | その場でOverlay（契約§3.1b）。**遷移しない** |
| ② | BottomNav「探す」 | `/search` へ移動。**自動focusなし** |
| ③ | 確定 | `/search?q=` へ移動 |

Overlay の空欄時は**最近の検索のみ**。トレンド表示は置かない。

---

## /search トップ — 探し方4枚 ＋ 開閉軸

ロゴ → SearchHeader（検索欄統合・戻るなし）→ 最近の検索 → **探し方4枚** →
選んだ探し方のパネル（同一画面）→ 固定バー「この条件で検索」。

### 探し方 4枚（`data-how`）

| 値 | ラベル |
|---|---|
| `cond` | 条件で探す |
| `pn` | 型番で探す |
| `myrig` | 自分のRIGから探す |
| `user` | 人・ガレージを探す |

### 「条件で探す」の開閉軸 5本（`data-ax`）

| 値 | 見出し | 件数 |
|---|---|---|
| `type` | 種別 | 4（RIG/パーツ/LOG/製品情報。**ユーザーは含まない**＝探し方カード側） |
| `scat` | 車種カテゴリ | 24 |
| `pcat` | パーツカテゴリ | 親14 |
| `maker` | メーカー | — |
| `scale` | スケール | 13（`data-size-class`。※値集合はHOLD） |

「すべて開く」トグルあり。

---

## 検索結果

- **SearchHeader**（戻る = fallback `/search`）
- **種別6本・件数つき**（すべて/RIG/パーツ/LOG/製品情報/ユーザー）。実装は `.filter-chip` のチップ列
- **絞り込みSheet** ＋ 件数バッジ ＋ 適用中条件チップ

### ソート（2026-08-21 裁定⑦⑧）

| 面 | 挙動 |
|---|---|
| 「すべて」タブ | **ソート行を出さない**（横断ソートは定義不能） |
| 種別タブ | 単一ドロップダウン |

**「人気順」は使わない。** 何で並んでいるかを名指しする。

| 種別 | 選択肢 |
|---|---|
| RIG | 関連順 / 新着順 / いいね順 |
| パーツ | 関連順 / 装着例が多い順 / 新着順 |
| LOG | 関連順 / 新着順 / いいね順 |
| 製品情報 | 関連順 / 登録RIGが多い順 / 新着順 |
| ユーザー | 関連順 / RIG登録数順 |

既定は全種別「関連順」。

### 「すべて」= ダイジェスト29件

RIG8 / パーツ8 / 製品4 / LOG6 / ユーザー3。表示順は rig → parts → lib → log → user。
各セクションに「◯◯の結果をすべて見る →」。**無限にしない**（概観の役割）。

### 種別タブ = 無限スクロール

6件ずつ追加。ページ送りUIは無い。
6件に1枚の**フルワイド注目枠**（FEATURED・検索結果とは別枠）。**ユーザータブには差し込まない**。

### 表示形式

RIG / パーツ / 製品情報 = 2列グリッド。LOG / ユーザー = リスト行。空状態 = `.m-empty`。

### 広告

**検索面は広告ゼロ**（2026-08-21 裁定①）。

---

## 色

- **検索ハイライトは `--cat-*` 不使用。** 汎用accent系 `--color-link`
- **種別チップの選択色は `--cat-*` が正**（カテゴリ表現そのものなので流用ではない）

---

## スコープ定義

実装 `search-results.html` の定数群が一元定義。

| 定数 | 内容 |
|---|---|
| `LABEL` | RIG / パーツ / LOG / 製品情報 / ユーザー |
| `UNIT` | 台 / 点 / 件 / 件 / 人 |
| `GRIDTYPE` | rig, parts, lib = グリッド |
| `DIGEST` | rig8, parts8, lib4, log6, user3 |
| `SORT_OPTIONS` / `SORT_DEFAULT` | 上記のとおり |

**プレースホルダ基準文言: `RIG・パーツ・LOG・製品情報を検索`**
モバイルは探し方でスコープ連動（型番 → `型番・製品番号を入力（例: TRX-4）`、
ユーザー → `ユーザー名・ガレージ名を検索`）。

---

## 裁定済み・未実装

✅ **2026-08-22 追加**（GPT指摘で発覚）。2026-08-21 のイタヤ裁定8件のうち、①③⑦⑧は本書に反映済みだが、
**②④⑤⑥は`_proposals/search-results-ux-v1.md`にしか記載がなく、正典から欠落していた。**
`_proposals/`はPROPOSAL扱い（正典扱い禁止）のため、「裁定した事実」ごと参照不能になっていた。
以下は**裁定済み＝確定事項**であり、提案ではない。実装フェーズが来たら本節から本文へ移す。

| # | 裁定内容 | Phase | 根拠 |
|---|---|---|---|
| ② | **PC版のブレンド結果を種別セクション制へ直す**（モバイルの型別セクションが正） | PC版バッチ | 異種エンティティのスコアは比較不能（Lalmas / Arguello。1,296セッション実験で non-blended が適切と結論）。画面幅の問題ではない |
| ④ | **「すべて」の先頭に製品マスタの「答えカード」を1枚出す** | Phase 3 | 検索の主目的が製品理解のとき、行の羅列では答えにならない。Discogs / Rebrickable / Ravelry が同型 |
| ⑤ | **絞り込み候補チップ**（答えカード直下。押すとクエリが書き換わる） | Phase 3 | 「結果内キーワード絞り込みボックスは作らない」（クエリの所在が2箇所に割れる）の代替。GPT案の統合形 |
| ⑥ | **「この検索を保存」→ 新着通知** | Phase 4 | 再訪の理由を作る。ヤフオク・メルカリ・eBayが保有。認証ガードと束ねる必要あり |

※②はPC検索バッチの中心。④⑤はデータ側の集計、⑥は認証が前提になる。

### 検索基盤の実装原則（2026-08-23 裁定・未実装）

Next.js実装時の検索基盤は、**ページごとに独立した検索エンジンを作らず、共通の Search Service を1つ持つ**。
検索面ごとの差は、検索対象indexとranking/filter profileの切替で表現する。

- **Community index**: RIG / ユーザー登録PARTS / LOG を対象。標準検索の主対象。
- **Catalog index**: Research所有のmasterデータを検索用ドキュメントへ同期・索引化したもの。Library検索の主対象。
- **User index**: profile / handle 等のユーザー検索用。
- **Search profile**: Global / Library / User / Registration / Feed内検索等で、対象index・重み・フィルター・ソートを切り替える。
- query正規化、alias/synonym解決、typo tolerance、公開可否/権限フィルター等の共通処理はSearch Service側へ集約し、各ページへ重複実装しない。
- Feedの通常表示（おすすめ/フォロー中等）は検索ではなく推薦・ランキング責務として分離する。将来Feed内自由語検索を設ける場合は共通Search Serviceを利用する。

**未確定のまま残すもの:** Meilisearch / Typesense / Algolia / PostgreSQL FTS 等の具体製品選定、index物理構成、同期方式、関連度の数式・重み。実装時に比較・実測して決める。
この原則は「同じ語なら全画面で同じ順位になる」という意味ではない。検索面ごとにranking profileは変えてよいが、検索基盤と正規化ロジックは共通化する。

---

## 未確定

- **関連度の算出定義** — 実装もPhase2送り
- **種別チップ順（log→lib）とダイジェスト順（lib→log）の食い違い** — どちらに揃えるか。
  **これは将来議論項目ではなく裁定待ち**（`_audit`側で「裁定1点必要」と記録）
- **`size_class` / `event_tags` の値集合** — CURRENT.md のHOLD参照（将来議論項目）
- **「人気」バッジ**（結果ページのFEATURED枠）— ranking全廃方針との整合。CURRENT のモック是正キュー参照
