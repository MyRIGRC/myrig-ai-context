# 引き継ぎ — 正典整理 完了報告（2026-08-21〜22）

**最終 revision: MYRIG-20260822-013**

前スレッドの引き継ぎ書「docs/12本の本文精査」から始まり、正典⇄モックの往復まで到達した。
**次に読む人（別スレッドのAI含む）は、この文書ではなく `_AI/MyRIG_CURRENT.md` から読むこと。**
本書は「何をやったか・何を残したか」の記録。

---

## 完了したこと

| # | 内容 | commit |
|---|---|---|
| 1 | docs/12本の本文精査（3AIクロスチェック＋反証確認4周） | 007 |
| 2 | GPT外部監査の記録 | 008 |
| 3 | 正典⇄モックアップの全面照合 | 009 |
| 4 | 正典を実装へ追随 | 010 |
| 5 | モック是正A（見た目不変7件） | 011 / mockup b1f2640 |
| 6 | シェイプアップ＋拘束力レベル（L1/L2）の明示 | 012 |
| 7 | モック是正B（局所的な範囲） | 013 / mockup 5c8fa55 |

---

## 一番大事な変更 — 拘束力レベル

**docs/ 12本の冒頭に L1 / L2 を明示した。**

| | 本数 | 意味 |
|---|---|---|
| **L1** | 2本 | 逸脱禁止。design-nogo-list（提案打ち切りが目的）/ db-schema-answers（Research裁定） |
| **L2** | 10本 | **出発点。より良い案があれば提案する** |

CORE.md に「L2への提案は禁止事項ではなく**義務**」と書いた。
モックアップ制作フェーズは議論しながら作る段階であり、
正典に書いてあるからという理由で沈黙するのは避けたい失敗そのものだから。

---

## この2日で分かった構造的な問題

### ① 文書だけ見ても判定できない

`log_type` を「schema(4値) ⇔ UI(5値) の矛盾・要裁定」と診断したが、**実装を見たら誤診**だった。
5値目の実装値は `setting` ではなく **`setup`**（schema v1.2 で廃止済み）。
同じモック内の `feed-v3` が「廃止された値」と注記していた。**裁定不要のバグ**だった。

同様に、操作色 `--color-action-primary` は「実値の記録が見つからない」としていたが、
`mobile-shell.css` に実装済みだった。

**→ 「正典にこう書いてある」は、実装を見るまで判定材料にならない。**

### ② 監査AIを増やすほど安全とは限らない

Gemini は読めていないZIPについて**11件の指摘をでっち上げ**、指摘されるまで黙っていた。
固有名詞を全件検証してrepo内ヒット0件、偶然一致もゼロ。形式が本物そっくりだったため、
裏取りせず反映していたら正典を壊していた。

**→ 指摘は必ず現物で裏取りしてから採用する。**

### ③ 一次記録を読まずに二次記録で判断すると外す

`_decisions/2026-08-21_db-inquiry-002-realdata.md`（実DB確認）を読まずに
size_class / event_tags を判定して間違えた。反証確認がなければそのまま正典に書き込んでいた。

---

## 残っていること

すべて `_AI/MyRIG_CURRENT.md` に索引がある。

| 区分 | 件数 | 誰が |
|---|---|---|
| **モック是正キューB** | 11 | 実装作業。見た目が変わるため確認フローとの調整が要る |
| **GPT最優先** | 3（Dは解消） | 設計裁定。Next.js実装前 |
| **HOLD** | 6 | イタヤ裁定 or Research回答待ち |

### 特に重いもの3つ

1. **RLSがprivateデータを保護していない** — `pins` は「非公開」と定義されているのにRLSは全公開。
   `images` / `comments` も親の公開可否を検査していない。**UI非表示はアクセス制御にならない。**
   Next.js実装前に必ず解消すること
2. **物理DELETE禁止 ⇔ 解除手段の不在** — likes/favorites/pins/follows に soft-delete 列がなく、
   いいね解除が物理DELETEでしか実現できない。CORE（L1）の改訂を伴う
3. **surface/weather の登録経路が無い** — 検索側は10値/6値で確定しているのに
   PC composer は自由テキスト1本。**データが永久に入らない構造**

### やらなかったこと（判断）

- **v8カラーのPC展開（34ファイル）** — PC全体の色が変わる。確認済み6ページの再確認が
  必要になる可能性があり、モックアップ確認フローを止めうる
- **PC Feed の #28適用** — タブ構成・Lightbox・無限スクロールで範囲が広く、実質は実装作業
- **48px タップ領域** — 試みたが撤回。`.rc__chip` は `<a>` で `.rc__x` はその子のため
  `::after` が×を覆って削除が壊れる。親が `overflow-x:auto` で高さ30pxにクリップするため
  48pxにも達しない。見た目を変えずには対応不可

---

## 検索設計レーンへの回答（済）

| 照会 | 結果 |
|---|---|
| log_type 5値の2026-07-28裁定書 | **本repo配下に存在しない。** 5値主張の3文書はすべて `_proposals/` の二次記述 |
| cross-ref v4 が4値を確定とした元決定 | **再裁定ではなく転記。** 初出は cross-ref v1（2026-08-19）の「App側で表示に使っているもの（実測）」 |
| size_class 13値の最古文書 | 同じく cross-ref v1。**裁定値ではなくモックUIの実測値** |
| mini-micro の移行先 | 裁定記録なし。size_class と連動して宙に浮く |
| Knowledge削除で消えたファイル | **消失ゼロ。** 37本全件の行き先を現物照合で確認 |

---

## 運用メモ

- **repoは2つ**: `myrig-ai-context`（正典）/ `myrig-mockup`（モック）
- **push はイタヤさん手動**（Coworkのサンドボックスに GitHub 認証が無い）
- **push後に Claude Project で Sync now** すると通常チャットからも読める
- Cowork の**ローカルセッションはそのMacでしか開けない**。iPhone・ブラウザからは見えない。
  外から見るならGitHubのrepoを直接見る
